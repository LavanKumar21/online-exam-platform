name: Deploy

on:
  push:
    branches:
      - main

jobs:
  dummy-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment placeholder
        run: echo "Deployment workflow executed!"
